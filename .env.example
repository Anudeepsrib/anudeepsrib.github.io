# This portfolio currently builds as a static export and does not require secrets.
NEXT_PUBLIC_SITE_URL=https://anudeepsri.com

# Server-only placeholders for future API routes or build-time data jobs.
# Do not prefix private tokens with NEXT_PUBLIC_ and do not expose them to client code.
GITHUB_TOKEN=
OPENAI_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
